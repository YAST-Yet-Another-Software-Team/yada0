Lifecycle chip for a delivery/courier request — the core status indicator threaded through the tracking flow.

```jsx
<StatusPill status="en_route" />
```

Statuses: `searching` (pulses — real waiting state), `assigned`, `en_route`, `arrived`, `delivered`, `cancelled`. Requires the Iconify web component script loaded (see assets/README.md).
