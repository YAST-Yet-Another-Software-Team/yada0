Transient system message — "No riders available nearby", "Request sent". Dark surface so it reads above the map at any location.

```jsx
<Toast tone="error" onClose={dismiss}>No riders available nearby. Try again in a few minutes.</Toast>
```
