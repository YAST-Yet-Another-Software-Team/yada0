Small uppercase chip for generic labels — order numbers, counts, tags.

```jsx
<Badge tone="primary">New</Badge>
```

Tones: `neutral`, `primary`, `success`, `warning`, `danger`. For courier/order lifecycle state, use StatusPill instead — it carries an icon + more prominent type.
