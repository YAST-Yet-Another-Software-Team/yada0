Text input for addresses, phone numbers, search — used throughout the request flow.

```jsx
<Input label="Pickup address" placeholder="123 Main St" icon={<iconify-icon icon="lucide:map-pin"></iconify-icon>} />
```

Pass `error` for validation messages (renders in danger red below the field). `disabled` for read-only states.
